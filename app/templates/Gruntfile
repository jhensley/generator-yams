'use strict';

// load the config BEFORE we initialize grunt to make certain we load the config with the right node environment
var config = require('config'),
    globalGrunt = require('presto-framework').grunt;

module.exports = function(grunt) {
    // Project configuration.
    globalGrunt.init(grunt);
};