'use strict';

process.env.NODE_CONFIG_DIR = __dirname + '/node_modules/yams-framework/config/bootstrap';
var globalGrunt = require('yams-framework').grunt;

module.exports = function(grunt) {
    // Project configuration.
    globalGrunt.init(grunt);
};
