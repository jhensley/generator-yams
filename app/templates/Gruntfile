'use strict';

process.env.NODE_CONFIG_DIR = __dirname + '/node_modules/presto-framework/config/bootstrap';
var globalGrunt = require('presto-framework').grunt;

module.exports = function(grunt) {
    // Project configuration.
    globalGrunt.init(grunt);
};
