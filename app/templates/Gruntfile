'use strict';

var globalGrunt = require('presto-framework').grunt,
    // load the config after presto-framework.grunt, but BEFORE we grab grunt since grunt sets environment
    // variables necessary to load the configs properly
    config = require('config');

module.exports = function(grunt) {
    // Project configuration.
    globalGrunt.init(grunt);
};